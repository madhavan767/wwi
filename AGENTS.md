> [!IMPORTANT]
> This project is connected to a remote service. Avoid rewriting published git history — force pushing, rebasing, amending, or squashing commits that are already pushed may disrupt the sync workflow.
>
> Keep the active branch in a working state so commits can sync cleanly.
